var toolSelector = document.getElementById('tooltip')
var tooltip = new bootstrap.Tooltip(toolSelector)

var myCarousel = document.querySelector('#carouselCidades')
var carousel = new bootstrap.Carousel(myCarousel, { interval: 10 })
